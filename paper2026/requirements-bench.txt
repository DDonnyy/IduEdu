# Benchmark environment for the IduEdu paper — pip route, WITHOUT pyrosm.
#
# ⚠ pyrosm cannot be installed this way on any platform: its dependency cykhash
# publishes no wheels at all, so pip always compiles it and needs a C toolchain.
# Use ../environment-bench.yml (conda-forge) to get every arm in one environment;
# this file is the fallback when only pip is available and the pyrosm arm is
# knowingly skipped.
#
# Install into a dedicated venv, NOT the main project venv:
#   cd paper
#   uv venv .venv-bench --python 3.11
#   uv pip install --python .venv-bench/Scripts/python.exe -r requirements-bench.txt
#
# iduedu itself is installed editable from the repo root (paper/ is a direct child).
-e ../

# graph-construction competitors
osmnx>=2.0
# pyrosm intentionally absent here — see the note above and environment-bench.yml
cityseer               # pedestrian networks from a polygon; walk arm of B1
osmium>=3.7            # PBF header bbox extraction

# OD / shortest-path competitors
networkx>=3.3
networkit>=11.0
igraph>=0.11
rustworkx>=0.15        # Rust core; one Dijkstra per source, no batched OD API

# harness + figures
psutil>=5.9
pyarrow>=19.0
matplotlib>=3.10
seaborn>=0.13
jupyter>=1.1
objectnat==2.0.0
